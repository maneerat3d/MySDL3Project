# ===================================================================
# .gitignore for C++/CMake/SDL3 Projects by Jo SDL V3
# ===================================================================

# --- Build Artifacts & CMake ---
# ไม่นำโฟลเดอร์ที่ใช้ build ทั้งหมดเข้า Git
/build/

# ไฟล์ที่ CMake สร้างขึ้นระหว่างกระบวนการ (ถึงแม้ส่วนใหญ่จะอยู่ใน build/)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json
Makefile

# --- Compiled Binaries & Libraries ---
# ไฟล์ที่ได้จากการ compile ทั้งหมด
*.exe
*.dll
*.so
*.a
*.lib
*.obj
*.o
*.dylib

# --- IDE and Editor Configuration ---
# ไม่นำไฟล์ตั้งค่าของ Editor ส่วนตัวเข้า Git
# Visual Studio & VS Code
.vs/
.vscode/
*.sln
*.vcxproj*
*.suo
*.user
*.VC.db

# CLion / JetBrains
.idea/

# --- OS-specific files ---
# ไฟล์ของระบบปฏิบัติการที่ไม่เกี่ยวกับโปรเจกต์
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Log files ---
*.log
*.log.*

# --- Optional: Dependencies ---
# หากคุณไม่ต้องการ commit ไลบรารี SDL3 ที่ build แล้ว (เพื่อให้ repo เล็กลง)
# ให้ลบเครื่องหมาย # หน้าบรรทัด /lib/ ข้างล่างนี้
# ผู้ใช้จะต้อง build SDL3 เองตามเอกสารของคุณ
# /lib/